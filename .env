# 前端本身既不能直接读系统环境变量，也不能直接读 .env 文件，只有通过构建工具（如 CRA、Vite）注入的 “特殊命名” 变量，才能在代码中访问。
# 前端能访问的变量，是构建工具（CRA、Vite 等）在打包时，从 .env 文件中筛选出来、并嵌入到代码里的字符串 —— 而 “特殊命名”（如 REACT_APP_、VITE_）就是构建工具的筛选规则

REACT_APP_BACKEND_URL=http://localhost:5000